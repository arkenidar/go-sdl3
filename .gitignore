# Runtime data written by cmd/crud-app next to whichever binary runs it.
crud-app-data.json

# Build output of `go build -buildmode=c-shared -o cffi/libgui.so ./cffi`,
# shared by every FFI example under examples/ (lua-use, python-use).
cffi/libgui.so
cffi/libgui.h
cffi/libgui.dylib
cffi/libgui.dll

# Smoke-test binary built into the repo root by the Windows CI job.
go-use.exe

# Python bytecode cache from running examples/python-use/demo.py.
__pycache__/
python/examples/crud_demo_data.json
